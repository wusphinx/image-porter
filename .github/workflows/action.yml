name: Image Porter

on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2
      
      - name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      
      - name: Pull Image
        run: |
          docker pull gcr.io/tekton-releases/github.com/tektoncd/pipeline/cmd/kubeconfigwriter:v0.18.0
          docker tag gcr.io/tekton-releases/github.com/tektoncd/pipeline/cmd/kubeconfigwriter:v0.18.0  wusphinx/tektoncd-kubeconfigwriter:v0.18.0
          docker push wusphinx/kubeconfigwriter:v0.18.0
          docker pull gcr.io/tekton-releases/github.com/tektoncd/pipeline/cmd/controller:v0.18
          docker tag gcr.io/tekton-releases/github.com/tektoncd/pipeline/cmd/controller:v0.18 wusphinx/tektoncd-controller:v0.18
          docker push wusphinx/tektoncd-controller:v0.18
          